[lints]
workspace = true

[package]
name = "robogen-dsl"
version = "0.1.0"
edition = "2021"

[dependencies]
robogen-domain = { path = "../robogen-domain" }
serde = { version = "1", features = ["derive"] }

